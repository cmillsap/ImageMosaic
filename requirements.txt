PyQt6==6.10.1
Pillow==10.2.0
# HEIC/HEIF decoding. 0.22 is the newest release that accepts Pillow 10.2;
# 1.x requires Pillow 11+.
pillow-heif==0.22.0
# DNG decoding via LibRaw.
rawpy==0.27.1
pytest==7.4.3
scikit-learn==1.3.2
numpy==1.26.2
# Must be opencv-CONTRIB: cv2.saliency (used by SaliencyDetector) is not in
# the base opencv-python package. Pinned to 4.x because 5.x requires numpy>=2,
# which is incompatible with the scikit-learn pin above.
opencv-contrib-python==4.10.0.84
